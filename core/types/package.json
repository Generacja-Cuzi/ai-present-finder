{
  "name": "@core/types",
  "version": "1.0.0",
  "private": true,
  "description": "Core types package for AI Present Finder microservices",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
