{
  "name": "ai-present-finder",
  "version": "1.0.0",
  "private": true,
  "description": "AI Present Finder - Full-stack application with microservices backend and React frontend",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean && rm -rf node_modules",
    "dev": "concurrently \"pnpm run services:dev\" \"pnpm run fetch:dev\"",
    "fetch:dev": "concurrently \"cross-env FETCH_PROVIDER=olx PORT=8011 pnpm run --filter @backend/fetch-microservice dev\" \"cross-env FETCH_PROVIDER=allegro PORT=8012 pnpm run --filter @backend/fetch-microservice dev\" \"cross-env FETCH_PROVIDER=ebay PORT=8013 pnpm run --filter @backend/fetch-microservice dev\" \"cross-env FETCH_PROVIDER=amazon PORT=8014 pnpm run --filter @backend/fetch-microservice dev\"",
    "format": "prettier -w --ignore-unknown .",
    "format:check": "prettier --check .",
    "install:all": "pnpm install",
    "lint": "pnpm -r --no-bail lint",
    "prepare": "husky || true",
    "services:dev": "pnpm -r --filter=!@backend/fetch-microservice --parallel dev",
    "start": "pnpm -r --parallel start",
    "test": "pnpm -r --no-bail test",
    "test:e2e": "pnpm -r --no-bail test:e2e",
    "typecheck": "pnpm -r --no-bail typecheck"
  },
  "lint-staged": {
    "*": "prettier -w --ignore-unknown"
  },
  "prettier": "@solvro/config/prettier",
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@solvro/config": "^2.0.7",
    "@trivago/prettier-plugin-sort-imports": "^5.2.2",
    "concurrently": "^9.0.1",
    "cross-env": "^7.0.3",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.3",
    "prettier": "^3.6.2",
    "prettier-plugin-packagejson": "^2.5.19",
    "prettier-plugin-tailwindcss": "^0.6.14"
  },
  "volta": {
    "node": "22.20.0"
  }
}
